import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import { Toaster } from '@/components/ui/toaster';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Pleiades Corporation - Advancing Space and AI Technology',
  description: 'Leading innovation in aerospace and artificial intelligence through Pleiades Aerospace and NovusAI divisions.',
  keywords: 'aerospace, AI, space technology, satellites, rockets, artificial intelligence',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className="dark">
      <body className={inter.className}>
        {children}
        <Toaster />
      </body>
    </html>
  );
}